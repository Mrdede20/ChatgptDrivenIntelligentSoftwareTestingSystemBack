The two programs are actually identical, just written slightly differently. They both implement the Shunting Yard algorithm to convert an infix expression to a postfix expression. Therefore, given the same input, they will produce the same output.